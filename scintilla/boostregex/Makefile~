### Generated by Winemaker 0.8.3
###
### Invocation command line was
### /usr/bin/winemaker --nomsvcrt --dll --lower-all -I../lexlib -I../src -I../include -icomdlg32 -ishell32 -ishlwapi -iuser32 -igdi32 -iadvapi32 -lboost-regex1.46 .


SRCDIR                = .
SUBDIRS               =
DLLS                  = getboostver.exe
LIBS                  =
EXES                  =



### Common settings

CEXTRA                =
CXXEXTRA              =
RCEXTRA               =
DEFINES               =
INCLUDE_PATH          = -I../lexlib \
			-I../src \
			-I../include \
			-I.
DLL_PATH              =
DLL_IMPORTS           = comdlg32 \
			shell32 \
			shlwapi \
			user32 \
			gdi32 \
			advapi32
LIBRARY_PATH          =
LIBRARIES             = -lboost-regex


### getboostver.exe sources and settings

getboostver_exe_MODULE= getboostver.exe
getboostver_exe_C_SRCS=
getboostver_exe_CXX_SRCS= BoostRegExSearch.cxx \
			UTF8DocumentIterator.cxx
getboostver_exe_RC_SRCS=
getboostver_exe_LDFLAGS= -mwindows
getboostver_exe_ARFLAGS=
getboostver_exe_DLL_PATH=
getboostver_exe_DLLS  = odbc32 \
			ole32 \
			oleaut32 \
			winspool \
			odbccp32
getboostver_exe_LIBRARY_PATH=
getboostver_exe_LIBRARIES= uuid

getboostver_exe_OBJS  = $(getboostver_exe_C_SRCS:.c=.o) \
			$(getboostver_exe_CXX_SRCS:.cxx=.o) \
			$(getboostver_exe_RC_SRCS:.rc=.res)



### Global source lists

C_SRCS                = $(getboostver_exe_C_SRCS)
CXX_SRCS              = $(getboostver_exe_CXX_SRCS)
RC_SRCS               = $(getboostver_exe_RC_SRCS)


### Tools

CC = winegcc
CXX = wineg++
RC = wrc
AR = ar


### Generic targets

all: $(SUBDIRS) $(DLLS:%=%.so) $(LIBS) $(EXES)

### Build rules

.PHONY: all clean dummy

$(SUBDIRS): dummy
	@cd $@ && $(MAKE)

# Implicit rules

.SUFFIXES: .cpp .cxx .rc .res
DEFINCL = $(INCLUDE_PATH) $(DEFINES) $(OPTIONS)

.c.o:
	$(CC) -c $(CFLAGS) $(CEXTRA) $(DEFINCL) -o $@ $<

.cpp.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.cxx.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.rc.res:
	$(RC) $(RCFLAGS) $(RCEXTRA) $(DEFINCL) -fo$@ $<

# Rules for cleaning

CLEAN_FILES     = y.tab.c y.tab.h lex.yy.c core *.orig *.rej \
                  \\\#*\\\# *~ *% .\\\#*

clean:: $(SUBDIRS:%=%/__clean__) $(EXTRASUBDIRS:%=%/__clean__)
	$(RM) $(CLEAN_FILES) $(RC_SRCS:.rc=.res) $(C_SRCS:.c=.o) $(CXX_SRCS:.cxx=.o)
	$(RM) $(DLLS:%=%.so) $(LIBS) $(EXES) $(EXES:%=%.so)

$(SUBDIRS:%=%/__clean__): dummy
	cd `dirname $@` && $(MAKE) clean

$(EXTRASUBDIRS:%=%/__clean__): dummy
	-cd `dirname $@` && $(RM) $(CLEAN_FILES)

### Target specific build rules
DEFLIB = $(LIBRARY_PATH) $(LIBRARIES) $(DLL_PATH) $(DLL_IMPORTS:%=-l%)

$(getboostver_exe_MODULE).so: $(getboostver_exe_OBJS) $(getboostver_exe_MODULE:.dll=.spec)
	$(CXX) $(getboostver_exe_LDFLAGS) -o $@ $(getboostver_exe_OBJS) $(getboostver_exe_LIBRARY_PATH) $(getboostver_exe_DLL_PATH) $(DEFLIB) $(getboostver_exe_DLLS:%=-l%) $(getboostver_exe_LIBRARIES:%=-l%)


